'use client';

import { ListWithCards } from "@/types";

interface ListContainerProps {
    bordId: string;
    data: ListWithCards[];
}

export const ListContainer = ({
    bordId,
    data
}: ListContainerProps) => {
    return (
        <div>
            ListContainer
        </div>
    );
};